---
name: aws-cost-analysis
description: Analyze AWS costs across multiple accounts from CUR 2.0 data and generate an interactive HTML report with service/resource breakdown, anomaly detection, and optimization recommendations.
disable-model-invocation: true
---

# aws-cost-analysis

First-time setup: configure account info (name, profile, S3 path, etc.) in `config.yaml`.

Read `config.yaml` and `references/INSTRUCTIONS.md`, then follow the workflow.

For multiple accounts, use the Agent tool for parallel execution (see the parallel execution section in INSTRUCTIONS.md).
